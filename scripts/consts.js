import path from "path"

export const SUPERVISOR_REPO_URL =
    "git@github.com:home-assistant/supervisor.git"

export const RAW_SOURCE_PATH = path.resolve("raw_source")

export const SUPERVISOR_PATH = path.join(RAW_SOURCE_PATH, "supervisor")
